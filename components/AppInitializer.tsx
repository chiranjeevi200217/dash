'use client';

import { useEffect } from 'react';

export default function AppInitializer() {
  useEffect(() => {
    // Placeholder for any client-side initialization (toasts, plugins, etc.)
  }, []);

  return null;
}
